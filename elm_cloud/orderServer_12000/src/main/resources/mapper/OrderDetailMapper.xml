<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.OrderDetailMapper">

	<insert id="saveOrderDetailetBatch" parameterType="OrderDetailet">
	    insert into orderdetailet(orderId,foodId,quantity) values
	    <foreach collection="list" item="od" separator=",">
	        (#{od.orderId},#{od.foodId},#{od.quantity})
	    </foreach>
	</insert>

	<select id="findByOrderIdList" parameterType="list" resultType="OrderDetailet">
		SELECT
		*
		FROM
		orderdetailet
		WHERE
		orderdetailet.orderId IN
		<foreach item="item" index="index" collection="list"
				 open="(" separator="," close=")">
			#{item}
		</foreach>
	</select>
</mapper>    